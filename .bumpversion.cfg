[bumpversion]
current_version = 0.1.0
commit = False
tag = False

# npm run release:<part> synchronizes package-lock.json before committing and
# tagging all three version files together. Do not replace dependency versions.
[bumpversion:file:package.json]
search = "version": "{current_version}"
replace = "version": "{new_version}"
